# SecBlog 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个炫酷黑客风的个人安全技术博客，展示 CTF Writeup 和漏洞分析

**Architecture:** 基于 Astro 5 SSG 框架，使用 TailwindCSS 4 实现自定义黑客风主题，React 19 用于交互组件（Matrix 数字雨、Glitch 文字、搜索框），Content Collections 管理 Markdown 文章，Pagefind 提供静态搜索，Giscus 提供评论功能

**Tech Stack:** Astro 5 / TailwindCSS 4 / React 19 / TypeScript / Pagefind / Giscus / pnpm

---

## 文件结构

```
.worktrees/secblog/
├── astro.config.mjs           # Astro 配置
├── tailwind.config.mjs        # TailwindCSS 配置
├── tsconfig.json              # TypeScript 配置
├── package.json               # 依赖管理
├── public/
│   ├── favicon.svg            # 网站图标
│   └── robots.txt             # 搜索引擎配置
├── src/
│   ├── content/
│   │   ├── config.ts          # Content Collections schema 定义
│   │   ├── ctf/               # CTF Writeup 文章
│   │   │   ├── hgame-2026-ez-sql.md
│   │   │   └── hgame-2026-web-unpack.md
│   │   └── vuln/              # 漏洞分析文章
│   │       └── cve-2026-xxxx-deserialization.md
│   ├── components/
│   │   ├── Navbar.astro       # 导航栏
│   │   ├── Footer.astro       # 页脚
│   │   ├── MatrixRain.tsx     # Matrix 数字雨 React 组件
│   │   ├── GlitchText.tsx     # Glitch 抖动文字 React 组件
│   │   ├── Scanlines.astro    # CRT 扫描线覆盖
│   │   ├── ArticleCard.astro  # 文章卡片
│   │   ├── CtfEventCard.astro # CTF 赛事卡片
│   │   ├── VulnCard.astro     # 漏洞卡片
│   │   ├── SkillCard.astro    # 技能卡片
│   │   ├── TagCloud.astro     # 标签云
│   │   ├── SearchBox.tsx      # 搜索框 React 组件
│   │   ├── Toc.tsx            # 侧边目录 React 组件
│   │   ├── GiscusComment.tsx  # Giscus 评论 React 组件
│   │   └── FilterBar.astro    # 分类筛选器
│   ├── layouts/
│   │   ├── BaseLayout.astro   # 基础布局（导航+页脚+全局样式）
│   │   └── ArticleLayout.astro # 文章布局（TOC+评论）
│   ├── pages/
│   │   ├── index.astro        # 首页
│   │   ├── ctf/
│   │   │   ├── index.astro    # CTF 列表页
│   │   │   └── [slug].astro   # CTF Writeup 详情页
│   │   ├── vuln/
│   │   │   ├── index.astro    # 漏洞分析列表页
│   │   │   └── [slug].astro   # 漏洞详情页
│   │   ├── about.astro        # 关于页
│   │   ├── search.astro       # 搜索页
│   │   └── 404.astro          # 404 页面
│   ├── styles/
│   │   └── global.css         # 全局样式 + TailwindCSS 指令
│   └── lib/
│       └── content.ts         # 内容查询辅助函数
└── docs/
    └── superpowers/
        ├── specs/
        │   └── 2026-05-09-secblog-design.md
        └── plans/
            └── 2026-05-09-secblog.md
```

---

### Task 1: 项目初始化与基础配置

**Files:**
- Create: `package.json`
- Create: `astro.config.mjs`
- Create: `tsconfig.json`
- Create: `tailwind.config.mjs`
- Create: `src/styles/global.css`
- Create: `public/favicon.svg`
- Create: `public/robots.txt`

- [ ] **Step 1: 初始化 Astro 项目**

在 worktree 目录中创建 Astro 项目：

```bash
cd d:\编程\.worktrees\secblog
pnpm create astro@latest . --template minimal --no-install --no-git --typescript strict
```

- [ ] **Step 2: 安装依赖**

```bash
pnpm add @astrojs/react @astrojs/tailwind react react-dom @types/react @types/react-dom
```

- [ ] **Step 3: 配置 astro.config.mjs**

```javascript
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  site: 'https://ttsec.dev',
  integrations: [react(), tailwind()],
  markdown: {
    shikiConfig: {
      theme: 'github-dark',
    },
  },
});
```

- [ ] **Step 4: 配置 tailwind.config.mjs**

```javascript
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        neon: '#00ff41',
        'neon-dim': '#00ff4133',
        'neon-mid': '#00ff4188',
        danger: '#ff0040',
        cyan: '#00f0ff',
        purple: '#b400ff',
        warn: '#ffe600',
        orange: '#ff6b00',
        bg: '#020a02',
        'bg-card': '#0a140a',
        'bg-card-hover': '#0f1f0f',
        border: '#00ff4115',
        text: '#c0ffc0',
        'text-dim': '#4a7a4a',
      },
      fontFamily: {
        orbitron: ['Orbitron', 'sans-serif'],
        rajdhani: ['Rajdhani', 'sans-serif'],
        fira: ['Fira Code', 'monospace'],
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 5: 创建全局样式 src/styles/global.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Fira+Code:wght@300;400;500;600;700&family=Orbitron:wght@400;500;600;700;800;900&family=Rajdhani:wght@300;400;500;600;700&display=swap');

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  background: #020a02;
  color: #c0ffc0;
  font-family: 'Rajdhani', sans-serif;
  cursor: crosshair;
}

::-webkit-scrollbar {
  width: 6px;
}

::-webkit-scrollbar-track {
  background: #020a02;
}

::-webkit-scrollbar-thumb {
  background: #00ff4133;
  border-radius: 3px;
}

::-webkit-scrollbar-thumb:hover {
  background: #00ff4188;
}

::selection {
  background: #00ff4133;
  color: #00ff41;
}
```

- [ ] **Step 6: 创建 public/favicon.svg**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="4" fill="#020a02"/>
  <text x="16" y="22" font-family="monospace" font-size="18" fill="#00ff41" text-anchor="middle" font-weight="bold">&gt;_</text>
</svg>
```

- [ ] **Step 7: 创建 public/robots.txt**

```
User-agent: *
Allow: /

Sitemap: https://ttsec.dev/sitemap.xml
```

- [ ] **Step 8: 验证项目可构建**

```bash
pnpm build
```

Expected: 构建成功，无错误

- [ ] **Step 9: 提交**

```bash
git add -A
git commit -m "feat: 初始化 Astro 项目与基础配置"
```

---

### Task 2: Content Collections Schema 与示例文章

**Files:**
- Create: `src/content/config.ts`
- Create: `src/content/ctf/hgame-2026-ez-sql.md`
- Create: `src/content/ctf/hgame-2026-web-unpack.md`
- Create: `src/content/vuln/cve-2026-xxxx-deserialization.md`
- Create: `src/lib/content.ts`

- [ ] **Step 1: 定义 Content Collections schema**

创建 `src/content/config.ts`：

```typescript
import { defineCollection, z } from 'astro:content';

const ctfCollection = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    date: z.date(),
    event: z.string(),
    category: z.enum(['Web', 'Pwn', 'Crypto', 'Reverse', 'Misc']),
    difficulty: z.enum(['Easy', 'Medium', 'Hard']),
    tags: z.array(z.string()),
    rank: z.number().optional(),
    team: z.string().optional(),
  }),
});

const vulnCollection = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    date: z.date(),
    type: z.enum(['CVE', '0day', '逻辑漏洞', '配置不当']),
    severity: z.enum(['Critical', 'High', 'Medium', 'Low']),
    status: z.enum(['patched', 'reported', 'unpatched']),
    cve: z.string().optional(),
    tags: z.array(z.string()),
  }),
});

export const collections = {
  ctf: ctfCollection,
  vuln: vulnCollection,
};
```

- [ ] **Step 2: 创建 CTF 示例文章 hgame-2026-ez-sql.md**

```markdown
---
title: "ez_sql — SQL 注入入门"
date: 2026-05-01
event: "HGAME 2026"
category: "Web"
difficulty: "Easy"
tags: ["SQL注入", "联合查询"]
rank: 3
team: "TTTeam"
---

## 漏洞发现

打开题目，是一个登录页面。尝试输入 `admin' or 1=1--` 发现存在 SQL 注入点。

使用 `order by` 判断列数为 3，联合查询发现关键字被过滤。

## 利用过程

```sql
-- 查看当前数据库
UNION SELECT 1,database(),3--
-- 结果: hgame_web
```

## Flag 获取

成功获取 Flag：`flag{ez_sql_1nject10n_b4sic}`

## 总结

本题是一道经典的 SQL 注入入门题，考察联合查询和绕过技巧。
```

- [ ] **Step 3: 创建 CTF 示例文章 hgame-2026-web-unpack.md**

```markdown
---
title: "web_unpack — 堆叠查询 + UDF 提权"
date: 2026-05-01
event: "HGAME 2026"
category: "Web"
difficulty: "Medium"
tags: ["SQL注入", "UDF提权", "RCE"]
rank: 3
team: "TTTeam"
---

## 漏洞发现

登录页面存在 SQL 注入点，`into outfile` 被禁用，但堆叠查询未被过滤。

```sql
SELECT user(), current_user();
-- 结果: root@localhost
```

## 利用思路

通过 UDF 提权执行系统命令：

```sql
CREATE FUNCTION sys_eval RETURNS string SONAME 'udf.dll';
SELECT sys_eval('cat /flag');
```

## Flag 获取

成功获取 Flag：`flag{st4ck3d_qu3ry_udf_pr1v3sc}`

## 总结

本题考察 SQL 堆叠查询 + MySQL UDF 提权的组合利用。
```

- [ ] **Step 4: 创建漏洞分析示例文章**

```markdown
---
title: "某框架反序列化链分析"
date: 2026-04-28
type: "CVE"
severity: "Critical"
status: "patched"
cve: "CVE-2026-XXXX"
tags: ["Java反序列化", "POP Chain", "RCE"]
---

## 漏洞背景

某开源框架在处理用户输入时未进行充分的反序列化校验，导致攻击者可以构造恶意序列化数据触发 RCE。

## Sink 点分析

通过代码审计定位到 `ObjectInputStream.readObject()` 调用点：

```java
public Object deserialize(byte[] data) {
    ByteArrayInputStream bais = new ByteArrayInputStream(data);
    ObjectInputStream ois = new ObjectInputStream(bais);
    return ois.readObject();
}
```

## POP Chain 构造

从 sink 回溯到 source，构造完整利用链：

```
HashMap.readObject() -> EqualsBean.hashCode() -> TemplatesImpl.equals() -> ...
```

## 总结

该漏洞影响范围广泛，建议升级到最新版本。
```

- [ ] **Step 5: 创建内容查询辅助函数 src/lib/content.ts**

```typescript
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';

export async function getAllCtfPosts(): Promise<CollectionEntry<'ctf'>[]> {
  const posts = await getCollection('ctf');
  return posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
}

export async function getAllVulnPosts(): Promise<CollectionEntry<'vuln'>[]> {
  const posts = await getCollection('vuln');
  return posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
}

export async function getCtfEvents(): Promise<Map<string, CollectionEntry<'ctf'>[]>> {
  const posts = await getAllCtfPosts();
  const events = new Map<string, CollectionEntry<'ctf'>[]>();
  for (const post of posts) {
    const eventName = post.data.event;
    if (!events.has(eventName)) {
      events.set(eventName, []);
    }
    events.get(eventName)!.push(post);
  }
  return events;
}

export async function getAllTags(): Promise<string[]> {
  const ctfPosts = await getCollection('ctf');
  const vulnPosts = await getCollection('vuln');
  const allPosts = [...ctfPosts, ...vulnPosts];
  const tags = new Set<string>();
  for (const post of allPosts) {
    for (const tag of post.data.tags) {
      tags.add(tag);
    }
  }
  return Array.from(tags).sort();
}
```

- [ ] **Step 6: 验证 Content Collections 可正常加载**

```bash
pnpm build
```

Expected: 构建成功，Content Collections schema 校验通过

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: 添加 Content Collections schema 与示例文章"
```

---

### Task 3: 基础布局与全局组件

**Files:**
- Create: `src/layouts/BaseLayout.astro`
- Create: `src/layouts/ArticleLayout.astro`
- Create: `src/components/Navbar.astro`
- Create: `src/components/Footer.astro`
- Create: `src/components/Scanlines.astro`

- [ ] **Step 1: 创建 Navbar 组件**

创建 `src/components/Navbar.astro`，包含：
- 左侧 `>_ TT` logo + 闪烁光标
- 中间导航链接：首页 / CTF / 漏洞分析 / 关于
- 右侧搜索快捷键 `[ Ctrl+K ]`
- 固定顶部，磨砂玻璃背景
- 当前页面链接高亮（通过 `Astro.url.pathname` 判断）

- [ ] **Step 2: 创建 Footer 组件**

创建 `src/components/Footer.astro`，包含：
- ASCII Art 签名
- 版权信息：`© 2026 TT · Powered by Astro`
- 霓虹绿色高亮关键词

- [ ] **Step 3: 创建 Scanlines 组件**

创建 `src/components/Scanlines.astro`，包含：
- 全局 CRT 扫描线覆盖效果
- `pointer-events: none` 不阻挡交互
- `position: fixed` 固定覆盖

- [ ] **Step 4: 创建 BaseLayout 布局**

创建 `src/layouts/BaseLayout.astro`，包含：
- HTML head：meta 标签、OG 标签、字体引入、全局样式
- Navbar
- `<slot />` 插槽
- Footer
- Scanlines
- View Transition 支持

- [ ] **Step 5: 创建 ArticleLayout 布局**

创建 `src/layouts/ArticleLayout.astro`，继承 BaseLayout，额外包含：
- 面包屑导航
- 文章头部（分类标签 + 标题 + 元信息）
- `<slot />` 内容区
- 标签列表
- Giscus 评论区占位

- [ ] **Step 6: 创建测试首页验证布局**

修改 `src/pages/index.astro`，使用 BaseLayout，写入临时内容验证布局正确。

- [ ] **Step 7: 验证布局渲染**

```bash
pnpm dev
```

打开浏览器确认导航栏、页脚、扫描线效果正常

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat: 添加基础布局与全局组件（Navbar/Footer/Scanlines）"
```

---

### Task 4: Matrix 数字雨与 Glitch 文字组件

**Files:**
- Create: `src/components/MatrixRain.tsx`
- Create: `src/components/GlitchText.tsx`

- [ ] **Step 1: 编写 MatrixRain 组件测试**

在 `src/components/__tests__/MatrixRain.test.ts` 中编写测试：
- 组件渲染不报错
- Canvas 元素存在

- [ ] **Step 2: 实现 MatrixRain 组件**

创建 `src/components/MatrixRain.tsx`，使用 React + Canvas API：
- 日文片假名 + 特殊字符下落动画
- 头部字符随机变白（更亮）
- 响应窗口大小变化
- 移动端降低粒子密度
- 组件卸载时清理 interval 和 resize listener

- [ ] **Step 3: 编写 GlitchText 组件测试**

在 `src/components/__tests__/GlitchText.test.ts` 中编写测试：
- 组件渲染文字内容
- data-text 属性正确设置

- [ ] **Step 4: 实现 GlitchText 组件**

创建 `src/components/GlitchText.tsx`，使用 CSS clip-path + animation：
- 接收 `text` 和 `tag` props
- `::before` 伪元素使用青色偏移
- `::after` 伪元素使用红色偏移
- Glitch 动画间歇触发（90% 时间静止，10% 时间抖动）

- [ ] **Step 5: 在首页中集成测试**

修改 `src/pages/index.astro`，在 Hero 区域使用 MatrixRain 和 GlitchText 组件

- [ ] **Step 6: 验证动效**

```bash
pnpm dev
```

确认数字雨动画流畅，Glitch 文字抖动效果正常

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: 添加 Matrix 数字雨和 Glitch 文字组件"
```

---

### Task 5: 首页完整实现

**Files:**
- Modify: `src/pages/index.astro`
- Create: `src/components/ArticleCard.astro`
- Create: `src/components/TagCloud.astro`

- [ ] **Step 1: 实现 ArticleCard 组件**

创建 `src/components/ArticleCard.astro`，包含：
- 分类标签（CTF 青色 / 漏洞红色）
- 日期
- 标题
- 摘要
- 标签列表
- hover 效果：左侧绿色指示条 + 标题变绿 + 微位移

- [ ] **Step 2: 实现 TagCloud 组件**

创建 `src/components/TagCloud.astro`，包含：
- 标签列表，热门标签字号更大
- hover 时 `#` 前缀出现 + 霓虹发光
- 点击跳转到搜索页

- [ ] **Step 3: 实现首页完整布局**

修改 `src/pages/index.astro`，包含：
- Hero 区域：MatrixRain 背景 + GlitchText 名字 + 打字机副标题 + 技能标签 + 社交链接
- 最新文章区：6 篇文章卡片网格
- 热门标签区：TagCloud 组件

- [ ] **Step 4: 验证首页**

```bash
pnpm dev
```

确认首页所有区域正确渲染，动效正常

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 实现首页完整布局（Hero/文章列表/标签云）"
```

---

### Task 6: CTF 列表页与详情页

**Files:**
- Create: `src/pages/ctf/index.astro`
- Create: `src/pages/ctf/[slug].astro`
- Create: `src/components/CtfEventCard.astro`
- Create: `src/components/FilterBar.astro`
- Create: `src/components/Toc.tsx`

- [ ] **Step 1: 实现 FilterBar 组件**

创建 `src/components/FilterBar.astro`，包含：
- 分类按钮列表
- 选中态高亮
- 客户端 JS 实现筛选切换

- [ ] **Step 2: 实现 CtfEventCard 组件**

创建 `src/components/CtfEventCard.astro`，包含：
- 排名徽章（前三名金色）
- 赛事信息
- 分类标签
- 可展开/折叠的 Writeup 列表
- 难度标签

- [ ] **Step 3: 实现 CTF 列表页**

创建 `src/pages/ctf/index.astro`，包含：
- 页面标题（红色主题）
- FilterBar
- CtfEventCard 列表（按赛事分组）

- [ ] **Step 4: 实现 Toc 组件**

创建 `src/components/Toc.tsx`，使用 React：
- 从页面 DOM 提取 h2 标题生成目录
- 当前章节高亮（IntersectionObserver）
- 点击平滑滚动
- 移动端隐藏

- [ ] **Step 5: 实现 CTF 详情页**

创建 `src/pages/ctf/[slug].astro`，使用 ArticleLayout：
- 面包屑导航
- 文章头部（分类 + 标题 + 元信息）
- Markdown 渲染内容
- Toc 侧边目录
- Giscus 评论区

- [ ] **Step 6: 验证 CTF 页面**

```bash
pnpm dev
```

确认赛事卡片展开/折叠正常，详情页渲染正确

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: 实现 CTF 列表页与详情页"
```

---

### Task 7: 漏洞分析列表页与详情页

**Files:**
- Create: `src/pages/vuln/index.astro`
- Create: `src/pages/vuln/[slug].astro`
- Create: `src/components/VulnCard.astro`

- [ ] **Step 1: 实现 VulnCard 组件**

创建 `src/components/VulnCard.astro`，包含：
- 左侧严重度指示条（CRITICAL 红色脉冲 / HIGH 橙色 / MEDIUM 黄色）
- 漏洞分类标签（CVE 红 / 0day 紫 / 逻辑漏洞 黄 / 配置不当 青）
- CVE 编号
- 状态标签（已修复/已报告/未修复）
- hover 效果：标题变红 + 微位移

- [ ] **Step 2: 实现漏洞分析列表页**

创建 `src/pages/vuln/index.astro`，包含：
- 页面标题（红色主题）
- FilterBar（CVE/0day/逻辑漏洞/配置不当）
- VulnCard 列表

- [ ] **Step 3: 实现漏洞详情页**

创建 `src/pages/vuln/[slug].astro`，使用 ArticleLayout，与 CTF 详情页类似

- [ ] **Step 4: 验证漏洞分析页面**

```bash
pnpm dev
```

确认漏洞卡片严重度指示器脉冲动画正常，筛选功能正常

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 实现漏洞分析列表页与详情页"
```

---

### Task 8: 关于页

**Files:**
- Create: `src/pages/about.astro`
- Create: `src/components/SkillCard.astro`

- [ ] **Step 1: 实现 SkillCard 组件**

创建 `src/components/SkillCard.astro`，包含：
- 技能名 + 等级标签（EXPERT 红 / ADVANCED 紫 / INTERMEDIATE 青）
- 渐变进度条 + 发光效果
- 子技能标签

- [ ] **Step 2: 实现关于页**

创建 `src/pages/about.astro`，包含：
- 个人信息区：ASCII Art 头像 + 彩虹渐变边框 + 名字 + 角色 + 简介 + 社交链接
- 技能树：4 张 SkillCard
- CTF 战绩：4 个统计卡片 + 排名柱状图

- [ ] **Step 3: 验证关于页**

```bash
pnpm dev
```

确认技能进度条动画、统计数字、柱状图正常

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: 实现关于页（个人信息/技能树/CTF战绩）"
```

---

### Task 9: 搜索页与 Pagefind 集成

**Files:**
- Create: `src/pages/search.astro`
- Create: `src/components/SearchBox.tsx`

- [ ] **Step 1: 安装 Pagefind**

```bash
pnpm add pagefind
```

- [ ] **Step 2: 配置 Pagefind**

在 `astro.config.mjs` 中添加 Pagefind 构建后钩子

- [ ] **Step 3: 实现 SearchBox 组件**

创建 `src/components/SearchBox.tsx`，使用 React：
- 搜索输入框，聚焦时霓虹边框发光
- 实时搜索结果展示
- 关键词高亮
- Ctrl+K 快捷键唤起
- ESC 关闭

- [ ] **Step 4: 实现搜索页**

创建 `src/pages/search.astro`，包含：
- SearchBox 组件
- 搜索结果列表
- 空搜索状态提示

- [ ] **Step 5: 在 BaseLayout 中添加全局搜索快捷键**

修改 `src/layouts/BaseLayout.astro`，添加 Ctrl+K 全局监听

- [ ] **Step 6: 验证搜索功能**

```bash
pnpm build && pnpm preview
```

确认搜索框、结果高亮、快捷键正常

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat: 实现搜索页与 Pagefind 集成"
```

---

### Task 10: Giscus 评论与 404 页面

**Files:**
- Create: `src/components/GiscusComment.tsx`
- Create: `src/pages/404.astro`

- [ ] **Step 1: 实现 GiscusComment 组件**

创建 `src/components/GiscusComment.tsx`，使用 React + @giscus/react：
- 懒加载（IntersectionObserver 触发）
- 暗色主题适配
- 接收 repo/category 等配置 props

- [ ] **Step 2: 在 ArticleLayout 中集成 GiscusComment**

修改 `src/layouts/ArticleLayout.astro`，在底部添加 GiscusComment 组件

- [ ] **Step 3: 实现 404 页面**

创建 `src/pages/404.astro`，包含：
- 巨型 404 数字 + Glitch 抖动效果
- Kali 终端模拟（逐行打出命令）
- 扫描进度条 + 闪烁光标
- 返回首页按钮

- [ ] **Step 4: 验证评论和 404**

```bash
pnpm dev
```

确认评论区懒加载正常，404 页面动效正常

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 添加 Giscus 评论组件与 404 页面"
```

---

### Task 11: SEO 与性能优化

**Files:**
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `astro.config.mjs`

- [ ] **Step 1: 添加 SEO meta 标签**

修改 BaseLayout，为每个页面添加：
- 独立的 title / description
- Open Graph 标签（og:title, og:description, og:image, og:url）
- Twitter Card 标签
- canonical URL

- [ ] **Step 2: 配置 sitemap**

在 `astro.config.mjs` 中添加 `@astrojs/sitemap` 集成

- [ ] **Step 3: 添加 JSON-LD 结构化数据**

在文章详情页添加 Article 类型的 JSON-LD 数据

- [ ] **Step 4: 性能优化**

- 字体使用 `font-display: swap`
- 图片添加 `loading="lazy"`
- MatrixRain 组件在移动端降低粒子密度
- 确保 CSS 动画使用 `will-change` 优化

- [ ] **Step 5: Lighthouse 审计**

```bash
pnpm build && pnpm preview
```

使用 Lighthouse 审计，确认性能分数 > 90

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: 添加 SEO 优化与性能调优"
```

---

### Task 12: 最终验证与构建

**Files:**
- All files

- [ ] **Step 1: 完整构建测试**

```bash
pnpm build
```

Expected: 构建成功，无错误无警告

- [ ] **Step 2: 预览验证所有页面**

```bash
pnpm preview
```

逐一验证 7 个页面：
- 首页：Hero 动效 + 文章列表 + 标签云
- CTF 列表：赛事卡片 + 筛选
- CTF 详情：代码高亮 + TOC + 评论
- 漏洞列表：严重度指示器 + 筛选
- 漏洞详情：代码高亮 + 评论
- 关于：技能树 + 战绩
- 搜索：搜索功能 + 高亮
- 404：Glitch 效果

- [ ] **Step 3: 响应式验证**

在移动端视口下验证所有页面布局正常

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "feat: SecBlog v1.0.0 完成"
```
